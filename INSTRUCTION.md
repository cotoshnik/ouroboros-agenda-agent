# ИНСТРУКЦИЯ: запуск и работа с агентом подготовки аналитических справок

Версия 2 (v2). Проект Sber AI Hack — автономный агент на базе Ouroboros.
Документ описывает всё: от установки до ежедневной работы, включая
данные MCP-сервера, все инструменты, форматы файлов и типичные проблемы.

---

## 1. Что это такое

Агент `agenda_briefing_agent` автоматизирует подготовку аналитических
справок дежурной смены:

1. **Сам обходит открытые веб-источники** (RSS-ленты: РБК, РИА Новости,
   ТАСС, Интерфакс, Банк России), собирает публикации, размечает их по
   темам повестки и выделяет экстренные сигналы.
2. **Принимает внутренние показатели банка** (67 показателей из каталога:
   клиентопоток, наличность, КТД, инциденты и т.д.) — они поступают из
   внутренних источников (письма, инфопанель) и загружаются через
   инструмент `save_observation`.
3. **Собирает аналитическую справку** одним вызовом: базовый блок (49
   обязательных показателей) + динамический блок (по наличию данных) +
   сигналы + раздел веб-мониторинга + отчёт о полноте данных — и
   **сохраняет её в формате Word (.docx)**. Справка — финальный
   артефакт цикла: задача агента считается выполненной только когда
   .docx собран из реально полученных данных (контракт результата —
   `SKILL.md`, раздел 6).
4. Финальную верификацию, выводы и отправку делает **человек-аналитик**
   (human-in-the-loop). Агент ничего никуда не отправляет сам.

---

## 2. Требования

| Компонент | Требование |
|-----------|-----------|
| ОС | macOS 12+ / Windows / Linux |
| Python | 3.9 или новее (проверка: `python3 --version`) |
| Зависимости Python | **НЕТ** — сервер и коллектор на стандартной библиотеке |
| Ouroboros | v6.24.0+ с настроенным провайдером (OpenRouter / Cloud.ru / локальный) |
| Сеть | интернет для RSS-источников; **VPN не нужен** (все источники доступны из РФ) |
| Порт | 9999 свободен на localhost |

---

## 3. Установка

### Вариант А. Клонирование репозитория

```bash
git clone git@github.com:cotoshnik/ouroboros-agenda-agent.git
cd ouroboros-agenda-agent
```

### Вариант Б. Уже существующая локальная папка

```bash
cd /Users/mikhail/Documents/kimi/ouroboros_agent
```

Проверьте, что файлы на месте:

```bash
ls mcp/server.py mcp/collector.py memory/knowledge/indicators.json
```

---

## 4. Структура проекта (что за что отвечает)

```
ouroboros_agent/
├── SKILL.md                      # манифест навыка: AS IS, TO BE, политика автономии
├── README.md                     # краткое описание и быстрый старт
├── INSTRUCTION.md                # эта инструкция
│
├── mcp/                          # MCP-СЕРВЕР (сердце системы)
│   ├── server.py                 #   HTTP-сервер, 10 инструментов, порт 9999
│   ├── collector.py              #   веб-коллектор RSS (обход сайтов, разметка тем)
│   ├── docx_export.py            #   экспорт справки в Word .docx (без зависимостей)
│   └── mcp_servers.json          #   готовый конфиг для подключения в Ouroboros
│
├── agents/                       # A2A-агенты (роли процесса)
│   ├── collector.agent.json      #   сбор данных (веб + внутренние источники)
│   ├── analyst.agent.json        #   полнота, «горячие» темы, состав блоков
│   ├── editor.agent.json         #   сборка черновика, передача человеку
│   └── README.md                 #   схема взаимодействия и контракт handoff
│
├── memory/                       # ПАМЯТЬ И КОНТЕКСТ АГЕНТА
│   ├── identity.md               #   самоописание агента (кто, зачем, границы)
│   ├── scratchpad.md             #   рабочая память цикла (логи решений)
│   ├── dialogue_blocks.json      #   консолидированная история циклов
│   └── knowledge/
│       ├── indicators.json       #   КАТАЛОГ: 67 показателей (base/dynamic + критерии)
│       ├── sources.md            #   источники и окна отсечки (кто когда присылает)
│       └── report_template.md    #   шаблон справки (структура разделов)
│
├── safety/                       # БЕЗОПАСНОСТЬ
│   ├── BIBLE.md                  #   Конституция агента (P1–P7), неприкосновенна
│   └── review_policy.md          #   политика ревью (Advisory/Blocking)
│
└── data/                         # ДАННЫЕ (создаются и пополняются при работе)
    ├── web_sources.json          #   конфиг веб-источников и тематических словарей
    ├── observations_<дата>.json  #   РЕАЛЬНЫЕ внутренние показатели (только save_observation)
    ├── signals_<дата>.json       #   веб-сигналы за дату (создаёт коллектор)
    ├── report_draft_<дата>.md    #   черновики справок (markdown)
    └── report_draft_<дата>.docx  #   СПРАВКИ В WORD — финальный артефакт цикла
```

> **Про тестовые данные:** их в проекте НЕТ. Агент работает только с
> реальными данными: веб-сигналами из открытых источников и внутренними
> показателями, загруженными человеком через `save_observation`.
> Отсутствующее значение честно помечается «— нет данных —» (BIBLE.md, P2).

---

## 5. Запуск MCP-сервера — пошагово

### Шаг 1. Откройте терминал в папке проекта

```bash
cd /Users/mikhail/Documents/kimi/ouroboros_agent
```

### Шаг 2. Запустите сервер

macOS / Linux:

```bash
python3 mcp/server.py
```

Windows (PowerShell или cmd, из папки проекта):

```cmd
py mcp\server.py
```

(если `py` не найден — `python mcp\server.py`)

Ожидаемый вывод:

```
MCP-сервер agenda_briefing слушает http://127.0.0.1:9999/mcp
Инструментов: 10. Каталог: .../memory/knowledge/indicators.json
```

**Окно терминала не закрывайте** — сервер живёт, пока работает процесс.
Остановка: `Ctrl+C`.

Запуск в фоне (чтобы не держать окно):

```bash
nohup python3 mcp/server.py > mcp_server.log 2>&1 &
# остановить потом: pkill -f "mcp/server.py"
```

### Шаг 3. Проверьте, что сервер отвечает

Во втором окне терминала:

```bash
curl -X POST http://localhost:9999/mcp \
  -H 'Content-Type: application/json' \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}'
```

Правильный ответ:

```json
{"jsonrpc": "2.0", "id": 1, "result": {"protocolVersion": "2025-03-26",
 "capabilities": {"tools": {}}, "serverInfo": {"name": "agenda_briefing", "version": "0.1.0"}}}
```

---

## 6. Подключение сервера в Ouroboros

Путь в интерфейсе: **Settings → Advanced → MCP** → добавить сервер.

Заполните поля формы:

| Поле | Значение |
|------|----------|
| **Server ID / Name** | `agenda_briefing` |
| **Display name** | `Аналитические справки (локально)` — любая подпись |
| **Server URL** | `http://localhost:9999/mcp` |
| **Transport** | `streamable_http` |
| **Allowed tools** | оставить пустым (= все) или перечислить 10 штук через запятую: `list_indicators, get_selection_criteria, save_observation, get_observations, fetch_web_sources, get_signals, check_completeness, detect_hot_topics, build_report_draft, export_report_docx` |

Либо вставьте готовый JSON из файла `mcp/mcp_servers.json`:

```json
{
  "name": "agenda_briefing",
  "url": "http://localhost:9999/mcp",
  "transport": "streamable_http"
}
```

После сохранения Ouroboros **сам обнаружит инструменты** сервера — в чате
агент сможет их вызывать. Проверка: спросите агента «какие MCP-инструменты
тебе доступны?» — должен перечислить 10 штук.

> **Важно:** агент должен вызывать инструменты только через это
> MCP-подключение. Если агент вместо MCP-вызовов пытается запускать
> python-скрипты или импортировать `mcp/server.py` — значит, сервер не
> запущен или подключение не настроено. Запустите сервер (раздел 5) и
> передайте агенту: «используй только MCP-инструменты сервера
> agenda_briefing».

---

## 7. Данные MCP-сервера — полное описание

### 7.1. Каталог показателей — `memory/knowledge/indicators.json`

Единый источник истины по показателям. 67 записей:

```json
{
  "id": 26,
  "name": "Коэффициент технической доступности, %",
  "base_block": true,
  "dynamic_block": false,
  "selection_criterion": "Значение КТД берется за прошедшие сутки (письмо на почту присылается ориентировочно в 07:00-07:30)"
}
```

- `base_block: true` — показатель **обязателен** в каждой справке (49 шт.);
- `dynamic_block: true` — включается **при наличии данных** (18 шт.);
- `selection_criterion` — окно отсечки и источник (за какие сутки брать,
  во сколько приходит письмо). Агент обязан соблюдать эти окна.

### 7.2. Внутренние показатели — `data/observations_<дата>.json`

Значения за конкретные сутки. Создаётся инструментом `save_observation`:

```json
{
  "date": "2026-07-19",
  "observations": [
    {
      "indicator_id": 26,
      "name": "Коэффициент технической доступности, %",
      "block": "base",
      "value": 99.42,
      "unit": "%",
      "source": "письмо 07:00-07:30",
      "collected_at": "2026-07-19T07:31:12"
    }
  ]
}
```

Повторное сохранение по тому же `indicator_id` **перезаписывает** значение
(актуализация в течение дня).

### 7.3. Веб-сигналы — `data/signals_<дата>.json`

Создаётся коллектором при `fetch_web_sources`:

```json
{
  "date": "2026-07-20",
  "signals": [
    {
      "source_id": "cbr",
      "source_name": "Банк России",
      "title": "Активы на ИИС достигли 969 млрд рублей",
      "link": "https://www.cbr.ru/press/event/?id=28557",
      "published": "Mon, 20 Jul 2026 09:00:00 +0300",
      "summary": "...",
      "topics": ["finance"],
      "urgent": [],
      "collected_at": "2026-07-20T12:05:41"
    }
  ]
}
```

- `topics` — темы повестки, к которым отнесена публикация
  (`finance`, `cyber`, `security_svo`, `tech_ai`, `emergency`);
- `urgent` — сработавшие стоп-слова (теракт, кибератака, DDoS и т.п.);
  такие публикации попадают в блок «Экстренные сигналы» черновика.

Дедупликация по `link`: повторный сбор добавляет только новые публикации.

### 7.4. Конфиг веб-источников — `data/web_sources.json`

Здесь настраивается всё веб-поведение:

- `sources` — список RSS-лент. Добавить источник = добавить объект:
  ```json
  { "id": "kommersant", "name": "Коммерсантъ",
    "url": "https://www.kommersant.ru/RSS/news.xml", "type": "rss", "enabled": true }
  ```
  `"enabled": false` — временно отключить без удаления.
- `topics` — тематические словари: тема → список подстрок (ищутся в
  нижнем регистре в заголовке + тексте). Новая тема добавляется своим
  списком ключевых слов.
- `urgent_keywords` — стоп-слова для экстренных сигналов.
- `max_items_per_source` — лимит публикаций с источника (по умолч. 100).
- `request_timeout_sec` — таймаут одного источника (по умолч. 15).

### 7.5. Справки — `data/report_draft_<дата>.docx` (+ `.md`)

**Финальный артефакт цикла — Word-файл.** Создаётся инструментом
`export_report_docx`: заголовки, таблицы показателей, списки сигналов.
Всегда помечен «ЧЕРНОВИК» до верификации аналитиком. Markdown-версия
(`.md`) сохраняется рядом как промежуточный формат.

---

## 8. Все 10 инструментов — параметры и примеры

Обращение к инструменту напрямую (для тестов без Ouroboros):

```bash
curl -X POST http://localhost:9999/mcp \
  -H 'Content-Type: application/json' \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"<ИМЯ>","arguments":{<АРГУМЕНТЫ>}}}'
```

В чате Ouroboros достаточно сказать агенту по-человечески — он сам
выберет инструмент (примеры фраз даны ниже).

### 8.1. `list_indicators` — каталог показателей

| Параметр | Тип | Обяз. | Описание |
|----------|-----|-------|----------|
| `block` | string | нет | `base` / `dynamic` / `all` (по умолч. `all`) |

Пример: `{"name":"list_indicators","arguments":{"block":"base"}}` →
49 обязательных показателей с критериями отбора.
Фраза агенту: «Покажи показатели базового блока».

### 8.2. `get_selection_criteria` — окна отсечки и источники

Без параметров. Возвращает 53 показателя с заполненными критериями
(за какие сутки брать, когда приходит письмо).
Фраза агенту: «Когда приходят данные по IT-инцидентам?»

### 8.3. `save_observation` — загрузить значение показателя

| Параметр | Тип | Обяз. | Описание |
|----------|-----|-------|----------|
| `indicator_id` | int | да | id из каталога (1–68) |
| `date` | string | да | `ГГГГ-ММ-ДД` |
| `value` | number/string | да | значение |
| `unit` | string | нет | единица («млрд руб.», «%», «шт.») |
| `source` | string | нет | источник («письмо 06:30») |

Пример: `{"indicator_id":26,"date":"2026-07-20","value":99.87,"unit":"%","source":"письмо 07:15"}`
Фраза агенту: «Запиши: КТД за сегодня 99.87%, источник — письмо 07:15».

### 8.4. `get_observations` — что собрано за дату

| Параметр | Тип | Обяз. |
|----------|-----|-------|
| `date` | string | да |

Фраза агенту: «Что уже собрано за сегодня?»

### 8.5. `fetch_web_sources` — ЗАПУСК ВЕБ-ОБХОДА (главное)

| Параметр | Тип | Обяз. | Описание |
|----------|-----|-------|----------|
| `date` | string | нет | по умолчанию — сегодня |

Обходит все `enabled`-источники из `web_sources.json`, сохраняет новые
публикации в `data/signals_<дата>.json`. Ответ — статистика:

```json
{"new_signals": 275, "total_signals": 275,
 "sources": [{"id":"rbc","name":"РБК","fetched":30,"new":30}, ...],
 "errors": []}
```

`errors` не пустой → какой-то источник недоступен (см. раздел 11).
Фраза агенту: «Собери сигналы из открытых источников».

### 8.6. `get_signals` — выдача веб-сигналов

| Параметр | Тип | Обяз. | Описание |
|----------|-----|-------|----------|
| `date` | string | нет | по умолчанию — сегодня |
| `topic` | string | нет | `finance` / `cyber` / `security_svo` / `tech_ai` / `emergency` |
| `limit` | int | нет | по умолч. 50 |

Фраза агенту: «Покажи сигналы по кибербезопасности за сегодня».

### 8.7. `check_completeness` — контроль полноты базового блока

| Параметр | Тип | Обяз. |
|----------|-----|-------|
| `date` | string | да |

Возвращает, какие из 49 обязательных показателей ещё не поступили:
`{"complete": false, "missing_base": [{"id": 8, "name": "Остаток наличности..."}]}`
Фраза агенту: «Все ли базовые показатели собраны?»

### 8.8. `detect_hot_topics` — «горячие» темы по порогам

| Параметр | Тип | Обяз. |
|----------|-----|-------|
| `date` | string | да |

Проверяет собранные значения по пороговым правилам (`HOT_RULES` в
`mcp/server.py`): КТД < 99.5%, любые значимые инциденты, критичные
IT-инциденты, DDoS, план «Ковёр» и др.
Фраза агенту: «Есть ли горячие темы за сегодня?»

### 8.9. `build_report_draft` — сборка черновика

| Параметр | Тип | Обяз. |
|----------|-----|-------|
| `date` | string | да |

Собирает полный черновик (markdown) из 5 разделов. Ответ содержит поле
`draft_markdown` — готовый текст.
Фраза агенту: «Собери черновик справки за 2026-07-20».

### 8.10. `export_report_docx` — СОХРАНЕНИЕ СПРАВКИ В WORD (финальный шаг)

| Параметр | Тип | Обяз. |
|----------|-----|-------|
| `date` | string | да |

Собирает черновик и сохраняет его в двух форматах:

- `data/report_draft_<дата>.docx` — Word-документ (заголовки, таблицы,
  списки; открывается в MS Word / LibreOffice);
- `data/report_draft_<дата>.md` — markdown-версия.

Ответ: `{"docx_path": "...", "md_path": "..."}`.
Фраза агенту: «Сохрани справку за сегодня в Word».
**Это обязательный финальный шаг цикла** — без .docx задача не считается
выполненной (контракт результата, `SKILL.md` раздел 6).

---

## 9. Ежедневный рабочий цикл (runbook)

Тайминг привязан к расписанию источников (`memory/knowledge/sources.md`).

```
06:10  приходит письмо по кибербезопасности
06:30  письмо по наличности/УС/ВСП
07:05  IT-инциденты (окно 07:00→07:00)
07:30  КТД
08:00  инциденты безопасности
09:00  сводка СВО (окно 09:00→09:00)
09:05  ДЕДЛАЙН: базовый блок должен быть полным (Координационный Штаб)
```

### Сценарий с агентом (чат Ouroboros)

1. **Запустите сервер** (раздел 5) — один раз утром.
2. «Собери сигналы из открытых источников» → `fetch_web_sources`.
3. По мере прихода писем: «Запиши остаток наличности 512.3 млрд руб.,
   источник письмо 06:30» → `save_observation` (и так по каждому письму;
   в пилоте этот шаг парсится из почты автоматически).
4. «Проверь полноту» → `check_completeness`. Если есть пропуски —
   запросить источник или принять решение о пометке «нет данных».
5. «Есть горячие темы?» → `detect_hot_topics`.
6. «Собери черновик справки за сегодня» → `build_report_draft`.
7. «Сохрани справку в Word» → `export_report_docx`: готовый файл
   `data/report_draft_<дата>.docx`. **Цикл завершён только здесь.**
8. **Аналитик**: открывает .docx, проверяет цифры, добавляет выводы и
   интерпретацию, снимает пометку «ЧЕРНОВИК», утверждает и отправляет.
   Без этого шага материал никуда не уходит (BIBLE.md, P1).

### Повторные сборы в течение дня

`fetch_web_sources` можно запускать сколько угодно раз — добавляются
только новые публикации. Перед вечерней сводкой обновите значения через
`save_observation` и пересоберите черновик.

---

## 10. Прогон для защиты на хакатоне (только реальные данные)

Прогон строится исключительно на открытых источниках — тестовых данных
в проекте нет:

```bash
cd ouroboros_agent
python3 mcp/server.py                   # сервер (Windows: py mcp\server.py)
```

В чате агента:

1. «Собери сигналы из открытых источников» — реальный обход 5 сайтов.
2. «Сохрани справку за сегодня в Word» — `export_report_docx` собирает
   черновик из собранных сигналов и отдаёт готовый
   `data/report_draft_<дата>.docx` за секунды.
3. Если есть реальные значения внутренних показателей (из писем) —
   загрузите их фразами «Запиши КТД 99.87%, источник письмо 07:15» и
   пересоберите справку. Без них базовый блок честно покажет
   «— нет данных —» — это правильное, а не сломанное поведение.

Что показать жюри:

- AS IS: 135–200 минут ручной работы → TO BE: один цикл MCP-вызовов
  и готовый Word-документ.
- Живой веб-сбор: 400+ сигналов, разметка по темам, дедупликация,
  экстренные сигналы — всё из реальных RSS, без единой тестовой записи.
- Раздел сигналов: пороговые правила сами вытаскивают наверх КТД ниже
  нормы, DDoS-атаки, критичные инциденты.
- Пометка «ЧЕРНОВИК» и human-in-the-loop — безопасность по BIBLE.md.

---

## 11. Типичные проблемы и решения

| Симптом | Причина | Решение |
|---------|---------|---------|
| `Address already in use` при запуске | порт 9999 занят | `pkill -f "mcp/server.py"` или поменять `PORT` в `mcp/server.py` и в конфиге Ouroboros |
| Ouroboros не видит инструменты | сервер не запущен / неверный URL | проверить `curl`-тест из раздел 5; URL должен быть `http://localhost:9999/mcp` |
| `errors` в ответе `fetch_web_sources` | источник недоступен (таймаут/блокировка) | не страшно — остальные соберутся; при постоянной недоступности поставьте `"enabled": false` в `web_sources.json` |
| `new_signals: 0` | всё уже собрано ранее | нормально: дедупликация по ссылкам |
| «— нет данных —» в черновике | показатель не загружен | загрузить через `save_observation` или оставить пометку (честнее, чем выдуманное значение — BIBLE.md P2) |
| `date должен быть в формате ГГГГ-ММ-ДД` | неверный формат даты | только `2026-07-20`, не `20.07.2026` |
| `показатель с id=N не найден` | нет такого id в каталоге | допустимые id — из `list_indicators` |
| Агент запускает python-скрипты / импортирует `mcp/server.py` вместо MCP-вызовов | сервер не запущен или MCP-подключение не настроено | запустить сервер (раздел 5), проверить подключение (раздел 6), сказать агенту: «используй только MCP-инструменты сервера agenda_briefing» |
| `SAFETY_VIOLATION ... Reasoning is mandatory for this endpoint` | ошибка на стороне провайдера модели при проверке команд в Ouroboros — выполнение команд блокируется средой, **это не сбой проекта** | не пытаться обойти выполнением скриптов; работать через MCP-подключение (сервер уже запущен человеком). Если блокируется всё — сменить модель/провайдера в Settings → Models или отключить проверку команд, если политика позволяет |
| Агент написал сигналы/новости «от себя» | нарушение BIBLE.md P2 (фабрикация) | отклонить результат; потребовать честную пометку «Веб-сбор не выполнялся» и запуск `fetch_web_sources` через MCP |
| Skill review quorum failure в Ouroboros | одна ревью-модель | добавить ключ второго провайдера (гайд, раздел 11) |
| Медленный сбор | один источник «висит» | таймаут 15 сек на источник; худший случай = +15 сек, остальные не блокируются |

---

## 12. Кастомизация (что и где менять)

| Задача | Где менять | Ограничение |
|--------|-----------|-------------|
| Добавить RSS-источник | `data/web_sources.json` → `sources` | свободно |
| Добавить тему повестки | `data/web_sources.json` → `topics` | свободно |
| Стоп-слова экстренных сигналов | `data/web_sources.json` → `urgent_keywords` | свободно |
| Пороги «горячих» тем | `mcp/server.py` → `HOT_RULES` | **только через ревью** (BIBLE.md P4) |
| Каталог показателей | `memory/knowledge/indicators.json` | **только через ревью** (P4) |
| Шаблон справки | `memory/knowledge/report_template.md` + `build_report_draft` | **только через ревью** (P4) |
| Порт сервера | `mcp/server.py` → `PORT` + URL в конфиге Ouroboros | свободно |

После любых правок `server.py` / `collector.py` — перезапустить сервер.

---

## 13. Безопасность (коротко, полное — `safety/BIBLE.md`)

1. Агент **не отправляет** материалы — только человек (P1).
2. Агент **не выдумывает** значения: нет данных = пометка (P2).
3. Каждое изменение кода/правил — через ревью, откат через git (P3).
4. Неприкосновенны: `BIBLE.md`, каталог показателей, шаблон,
   пороговые правила, любые ключи/токены (P4).
5. Каждое автономное решение логируется в `memory/scratchpad.md` (P5).
6. Сеть — только утверждённые источники из `web_sources.json`;
   новый источник = подтверждение человеком (P6).
7. Режим `/evolve` запрещён; `/panic` доступен аналитику всегда (P7).

---

## 14. Метрики для отчёта (AS IS → TO BE)

| Метрика | AS IS | TO BE (замер) |
|---------|-------|---------------|
| Подготовка типовой справки | 135–200 мин | один цикл вызовов (~1–2 мин машинного времени) |
| Рутинная нагрузка команды | 10–18 чел-ч/день | цель 4–8 чел-ч/день |
| Доля автособранного черновика | 0% | 70–85% (разделы 1–2, 4–5 собираются полностью) |
| Веб-мониторинг | ручной обход сайтов | 275+ сигналов за ~10 сек, автоматическая разметка тем |

Как замерить: засечь время от «Собери сигналы» до готового черновика и
сравнить с хронометражем ручного цикла из SKILL.md (раздел AS IS).
